{# Phase 1 stub. Full prompt lands in Phase 6 (clarify node). #}
The system description leaves the attribute "{{ attribute }}" underspecified,
and that attribute is required to make a defensible classification decision.

Ask the user one short, targeted question to resolve it. Do not classify.
Do not list multiple questions. Do not speculate about the answer.

Why this attribute matters:
{{ why_it_matters }}

Original description:
{{ system_description }}
